[build]
builder = "NIXPACKS"
buildCommand = "npm install"

[deploy]
startCommand = "node dashboard/server.js & node src/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[[services]]
name = "d1z-bot"

[environments.production.variables]
NODE_ENV = "production"
